import { IPublicKey } from './types';
export declare class VirgilPublicKey implements IPublicKey {
    identifier: Uint8Array;
    key: Uint8Array;
    constructor(identifier: Uint8Array, key: Uint8Array);
}
