import { Data } from './types';
import { VirgilPublicKey } from './VirgilPublicKey';
export declare class VirgilStreamVerifier {
    private isDisposed;
    private verifier;
    constructor(signature: Data);
    update(data: Data): this;
    verify(publicKey: VirgilPublicKey, final?: boolean): any;
    dispose(): void;
}
