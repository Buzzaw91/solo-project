let helmet,bodyParser,jwt,config;_507‍.x([["default",()=>_507‍.o]]);_507‍.w("helmet",[["default",["helmet"],function(v){helmet=v}]]);_507‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_507‍.w("express-jwt",[["default",["jwt"],function(v){jwt=v}]]);_507‍.w("./index",[["default",["config"],function(v){config=v}]]);





const _507‍anonymous=app => {
  // always does auth with JWT, except the paths you write below

   /* app.use(
    jwt({secret: config.JWT_SECRET})
      .unless({
        // we have no JWT in login/signup etc. routes
        // so we tell app not to try auth with JWT
        path: [
          'api/users/login',
          'api/users/signup'
        ]
      })

  ) */
   app.use((req, res, next)=> {
    _507‍.g.console.log('\n PATH ',req.path)
    next()
  })

  app.use(bodyParser.json())
  app.use(bodyParser.urlencoded({extended: true}))

  app.use(helmet()) // for some out of the box security
};_507‍.d(_507‍anonymous);
