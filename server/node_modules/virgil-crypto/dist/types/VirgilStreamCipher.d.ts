/// <reference types="node" />
import { Data } from './types';
import { VirgilPublicKey } from './VirgilPublicKey';
export declare class VirgilStreamCipher {
    private _isFinished;
    private isRunning;
    private isDisposed;
    private recipientCipher;
    private messageInfoCustomParams?;
    private aes256Gcm;
    private ctrDrbg;
    private publicKeys;
    get isFinished(): boolean;
    constructor(publicKey: VirgilPublicKey | VirgilPublicKey[], signature?: Data);
    start(): Buffer;
    update(data: Data): Buffer;
    final(dispose?: boolean): Buffer;
    dispose(): void;
    private ensureLegalState;
    private ensureIsRunning;
}
