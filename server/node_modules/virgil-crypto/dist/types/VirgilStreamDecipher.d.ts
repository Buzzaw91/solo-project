/// <reference types="node" />
import { Data } from './types';
import { VirgilPrivateKey } from './VirgilPrivateKey';
export declare class VirgilStreamDecipher {
    private _isFinished;
    private isDisposed;
    private recipientCipher;
    private privateKey;
    get isFinished(): boolean;
    constructor(privateKey: VirgilPrivateKey);
    getSignature(): Buffer;
    update(data: Data): Buffer;
    final(dispose?: boolean): Buffer;
    dispose(): void;
    private ensureLegalState;
}
